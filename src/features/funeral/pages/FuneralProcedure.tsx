import React from 'react';

function FuneralProcedure({className} : {className:string}) {
    return (
        <div className={className}>
            장례절차
        </div>
    );
}

export default FuneralProcedure;
