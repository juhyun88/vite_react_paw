import React from 'react';

function StoneFuneral({className} : {className:string}) {
    return (
        <div className={className}>
            스톤 비용
        </div>
    );
}

export default StoneFuneral;